declare module "mocha-remote-mocha" {
  import { Context, Runner, Suite, interfaces } from "mocha";
  import type { Interface } from "mocha";
  export { Context, Runner, Suite, interfaces, Interface };
}